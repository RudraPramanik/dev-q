Embeddings + OpenAI Cookbook
Embeddings
1. What is an embedding?
2. Why do semantically similar texts produce similar vectors?
3. Why are embeddings generated offline instead of per request?
Embedding PipelineWhat happens from raw document → vector DB?
2. Where should chunking happen?
3. Where should embedding happen?
Updating Data
1. What happens when a document changes?
2. Why use content hashes?
Structured Outputs
2. Why are structured outputs better than free-form text?
3. What problems do JSON schemas solve?
Reliability
1. How would you handle retries?
2. How would you handle rate limits?
3. Why should embedding requests be batched?